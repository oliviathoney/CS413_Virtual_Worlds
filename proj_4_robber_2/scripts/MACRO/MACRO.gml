#macro tile_width 32

enum player_positions {up, middle, down}
enum car_positions {top_up, top_down, bot_up, bot_mid, bot_down}
enum player_states {running, jumping, moving, caught}
enum player_speeds {start_speed, faster, fastest, slower, slowest, stopped}